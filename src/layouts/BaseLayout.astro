---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Unsere Webseite' } = Astro.props;
const siteName = 'Mein Verein';
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} — {siteName}</title>
  </head>
  <body class="min-h-screen flex flex-col bg-white">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
    <!-- Netlify Identity Widget — verarbeitet invite/confirmation/recovery tokens -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
    <script is:inline>
      var netlifyIdentity = window.netlifyIdentity;
      if (netlifyIdentity) {
        var hash = window.location.hash;
        var hasToken =
          hash.indexOf('invite_token') !== -1 ||
          hash.indexOf('confirmation_token') !== -1 ||
          hash.indexOf('recovery_token') !== -1;

        netlifyIdentity.on('init', function (user) {
          // Widget ist jetzt bereit — erst jetzt open() aufrufen
          if (hasToken) {
            netlifyIdentity.open();
          }
          if (!user) {
            netlifyIdentity.on('login', function () {
              document.location.href = '/admin/';
            });
          }
        });
      }
    </script>
  </body>
</html>
