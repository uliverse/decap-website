[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback for the CMS admin
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
